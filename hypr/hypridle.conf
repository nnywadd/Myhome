listener {
    timeout = 300                                # 5 minutes of inactivity
    on-timeout = makoctl mode -a do-not-disturb  # Enable DND
    on-resume = makoctl mode -r do-not-disturb   # Disable DND when you return
}
